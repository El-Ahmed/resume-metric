import { Routes } from '@angular/router';
import { ResumeMetricWorkflow } from './resume-metric-workflow/resume-metric-workflow';

export const routes: Routes = [
  {
    path: '',
    component: ResumeMetricWorkflow,
  },
];
